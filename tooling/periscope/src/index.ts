/**
 * Public library entry for `@anthonycoffey/periscope`.
 *
 * Consumers that want to use the engines or libs programmatically (not via
 * the CLI) import from here. Subsequent commits in Phase A populate these
 * re-exports as commands and engines land.
 */

export const PERISCOPE_VERSION = '0.0.0-dev';
